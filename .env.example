# متغيرات البيئة للتشغيل المحلي
# SQLite عبر libsql (WASM) — لا حاجة لإعداد خادم قاعدة بيانات منفصل

# رابط قاعدة البيانات SQLite
DATABASE_URL="file:./dev.db"

# سر الجلسة — سلسلة عشوائية طويلة (≥32 حرف). إلزامي في الإنتاج:
# عند غيابه يرفض النظام توقيع الجلسات بدلاً من استخدام سر افتراضي ضعيف.
AUTH_SECRET=
